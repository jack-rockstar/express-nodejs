<%- include("../template/cabeza.ejs",{tituloWeb:'Pagina de inventario'});  %>
   <div class="container">
      <h1>Detalle de productos</h1>
      <% if (error) { %>
       <p><%=mensaje %> </p>
       <a href="/tienda" class="btn btn-outline-success d-grid gap-2 my-4">Volver</a>
      <% } %>
      <% if (!error) { %>
         <table class="table">
            <thead>
              <tr class="table-dark">
                <th scope="col">#</th>
                <th scope="col">Producto</th>     nhnj 
                <th scope="col">Descripcion</th>
                <th scope="col">precio</th>
                <th scope="col">Accion</th>
              </tr>
            </thead>
         <tbody>
                <tr class="table-info">
                    <td ><%= tienda.id %> </td>
                    <td><%= tienda.producto %> </td>
                    <td><%= tienda.descripcion %> </td>
                    <td><%= tienda.precio %> </td>
               </tr>
         </tbody>
        </table> 
      <% } %>
   </div>
    
<%- include("../template/footer.ejs");  %> 